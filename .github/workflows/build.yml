on: push
name: Main Workflow
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Set up JDK 1.11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'

      - name: Build
        run: gradle :clean :assembly

      - name: Test
        run: gradle :test

      - name: Report
        uses: dorny/test-reporter@v1
        if: always()
        with:
          report_paths: '**/build/test-results/test/TEST-*.xml'